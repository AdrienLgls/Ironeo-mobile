# Ralph Progress Log
Started: 2026-03-19
Feature: 01-react-native-migration

## Codebase Patterns

- Screens vivent dans `app/` (pas de sous-dossier `screens/`)
- TabNavigator importe depuis `./ScreenName` (relatif à app/)
- Types dans `types/` — un fichier par domaine (auth.ts, user.ts...)
- Services dans `services/` — pattern : `const { data } = await api.get<Type>('/endpoint'); return data`
- NativeWind : `bg-background` (#121212), `text-accent` (#EFBF04), `bg-white/[0.05]` pour surfaces
- ScrollView avec `contentContainerClassName` pour le padding interne
- État local : useState + useEffect dans le screen
- Pattern erreur : `.catch(() => setError('message')).finally(() => setLoading(false))`

---

## 2026-03-19 - US-001: Home screen stats header
- Fichiers : app/HomeScreen.tsx (modifié), types/user.ts (créé), services/userService.ts (créé)
- Stats : streak, workoutsThisWeek, totalWorkouts depuis GET /users/stats
- Loading avec ActivityIndicator color="#EFBF04"

---


## 2026-03-19 - US-002: Last session widget
- Fichiers : components/home/RecentSessionCard.tsx (créé), app/HomeScreen.tsx (modifié)
- Pattern composant : `interface Props { session: T; onPress: () => void }` 
- Promise.all pour fetch parallèle de plusieurs endpoints au mount
- Composants dans `components/<domaine>/ComponentName.tsx`
- formatDate helper local dans le fichier composant (pas de lib externe)
- Empty state : `bg-white/[0.03]` pour contraste léger

---

## 2026-03-20 - US-003: Next workout widget
- Fichiers : components/home/NextWorkoutCard.tsx (créé), types/user.ts (NextWorkout ajouté), services/userService.ts (getNextWorkout), app/HomeScreen.tsx (modifié)
- getNextWorkout retourne null si 404/erreur (try/catch silencieux — normal si pas de prochain workout)
- NextWorkoutCard affiche un CTA "Go" même si pas de workout planifié
- Éviter `cat >>` pour les services — utiliser Read + Edit pour garder les imports propres

---

## 2026-03-20 - US-004: Weight/goals progress widget
- Fichiers : components/home/ProgressWidget.tsx (créé), app/HomeScreen.tsx (modifié)
- Retourne null si pas de données weight → pas de widget vide affiché
- Progress bar : View avec style={{ width: `${pct}%` }} (style dynamique inline ok pour %)
- Utiliser python3 -c pour modifier HomeScreen quand Edit échoue (file-not-read issue)

---

## 2026-03-20 - US-005: Programs list screen
- Fichiers : app/WorkoutScreen.tsx (réécrit), types/workout.ts (créé), services/workoutService.ts (créé), components/workout/ProgramCard.tsx (créé)
- WorkoutScreen contient un Stack.Navigator (ProgramsList + ProgramDetail placeholder)
- FlatList avec ListHeaderComponent pour le titre
- ListEmptyComponent gère erreur ET état vide
- Utiliser `cat > file << 'ENDOFFILE'` quand Write échoue (file-not-read-yet)
- Types workout : Program, Exercise, WorkoutSet, SessionExercise, WorkoutSession

---

## 2026-03-20 - US-006: Program detail screen
- Fichiers : app/WorkoutScreen.tsx (ProgramDetailScreen remplace placeholder), components/workout/ExerciseCard.tsx (créé), types/workout.ts (ProgramExercise, ProgramDay, ProgramDetail), services/workoutService.ts (getProgramDetail)
- NativeStackScreenProps<ParamList, 'ScreenName'> pour typer les props de navigation
- `navigation.goBack()` pour le bouton retour custom
- `cat >> file << 'EOF'` ok pour append, mais les inline imports sont moches → préférer lire + éditer imports du fichier
- getProgramDetail réutilise la même route que getProgramById (GET /programs/:id)

---

## 2026-03-20 - US-007: Active session screen
- Fichiers : app/ActiveSessionScreen.tsx (créé), hooks/useWorkoutSession.ts (créé), app/WorkoutScreen.tsx (ActiveSession+PostSession dans stack)
- useWorkoutSession hook gère tout le state : currentExerciseIndex, currentSetIndex, completion
- `navigation.replace('PostSession', ...)` quand isComplete=true (pas de back vers session)
- Stack.Screen children prop pour passer des params custom au component (program object)
- Inline import dans le type : `import('../types/workout').ProgramDetail` ok pour les params de nav

---

## 2026-03-20 - US-008: Rest timer
- Fichiers : components/workout/RestTimer.tsx (créé), app/ActiveSessionScreen.tsx (modifié)
- Timer local : useEffect + setInterval, cleanup sur return
- progress bar = 1 - remaining/duration (de droite à gauche)
- showRestTimer state dans ActiveSessionScreen, après Done button → lance le timer, après timer → completeSet()
- restDuration vient de program.days[0].exercises[idx].restSeconds ?? 90

---
